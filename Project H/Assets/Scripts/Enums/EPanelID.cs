// Name in enum must be the same as in /UIPrefabs

// Keep the order, we might need it in some point,
// to handle multiple panels on same canvas.
public enum EPanelID
{
    None = -1,
    SplashScreen = 0,
    MainMenu = 1
}
